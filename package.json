{
  "name": "brewnanza",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "dev": "bunx convex dev & turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "scrape": "bun run --filter @brewnanza/scraper scrape",
    "embed": "bun run --filter @brewnanza/embedder embed",
    "vocab": "bun run --filter @brewnanza/embedder vocab"
  },
  "devDependencies": {
    "convex": "^1.17.4",
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  },
  "packageManager": "bun@1.1.42",
  "dependencies": {
    "fuse.js": "^7.1.0"
  }
}
